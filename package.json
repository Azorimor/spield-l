{
    "name": "spield",
    "private": true,
    "workspaces": {
        "packages": [
            "packages/*"
        ]
    },
    "devDependencies": {
        "eslint": "^7.21.0",
        "jest": "^26.6.3",
        "lerna": "^4.0.0",
        "typescript": "^4.2.3"
    },
    "scripts": {
        "build": "lerna run build --stream",
        "lint": "lerna run lint --stream --parallel",
        "test": "yarn jest --coverage"
    }
}